import { InventaireRessource } from './inventaire-ressource';

describe('InventaireRessource', () => {
  it('should create an instance', () => {
    expect(new InventaireRessource()).toBeTruthy();
  });
});
